---
import Button from "@components/button/Button.astro";
import { getLocaleFromUrl } from "@js/localeUtils";
import { useTranslations } from "@js/translationUtils";
import { Image } from "astro:assets";
import { Icon } from "astro-icon/components";

import { getLocalizedRoute } from "@/js/translationUtils";

const currLocale = getLocaleFromUrl(Astro.url);
const t = useTranslations(currLocale);

import heroImage from "@/assets/images/design-img-1-min.jpg";
---

<section
  id="hero-side-image"
  class="dark bg-background text-foreground relative flex min-h-[100svh] items-center overflow-hidden"
>
  <!-- Background Elements -->
  <div class="absolute inset-0 z-0" aria-hidden="true">
    <svg class="absolute h-full w-full opacity-7" xmlns="http://www.w3.org/2000/svg">
      <defs>
        <pattern id="grid-pattern-alt" width="40" height="40" patternUnits="userSpaceOnUse">
          <path d="M 40 0 L 0 0 0 40" fill="none" stroke="currentColor" stroke-width="1"></path>
        </pattern>
      </defs>
      <rect width="100%" height="100%" fill="url(#grid-pattern-alt)"></rect>
    </svg>
  </div>
  <div class="site-container relative h-full">
    <div class="grid h-full items-center gap-12 py-24 lg:grid-cols-12">
      <!-- Left Content -->
      <div class="space-y-8 lg:col-span-6">
        <h1
          class="font-heading text-foreground/90 text-4xl leading-tight sm:text-5xl md:text-6xl"
          data-mos="fade-right"
          data-mos-anchor="#hero-side-image"
        >
          <span
            class="from-primary to-foreground/90 bg-gradient-to-r bg-clip-text text-transparent"
          >
            Transforming
          </span>
          <br />
          Ideas into Reality
        </h1>

        <p
          class="description text-xl leading-relaxed opacity-80"
          data-mos="fade-right"
          data-mos-delay="0.2"
          data-mos-anchor="#hero-side-image"
        >
          {t("hero_description")}
        </p>

        <div
          class="flex flex-col gap-4 sm:flex-row"
          data-mos="fade-right"
          data-mos-delay="0.4"
          data-mos-anchor="#hero-side-image"
        >
          <Button href="#contact" arrow="right"> Start a Project </Button>
          <Button href={getLocalizedRoute(currLocale, `/projects`)} variant="ghost">
            View Work
          </Button>
        </div>
      </div>

      <!-- Right Content -->
      <div class="relative lg:col-span-6">
        <div class="relative mx-auto aspect-square max-w-2xl">
          <!-- Geometric Overlay -->
          <div
            class="from-primary/20 absolute inset-0 rounded-full bg-gradient-to-br to-transparent blur-3xl"
          >
          </div>

          <!-- Main Image Container -->
          <div class="relative h-full">
            <!-- <div class="absolute inset-0 bg-gradient-to-t from-background/80 to-transparent z-10"></div> -->
            <div class="animate-float h-full overflow-hidden rounded-2xl">
              <Image
                src={heroImage}
                alt="Creative workspace showcasing design and development environment"
                class="z-10 h-full w-full object-cover"
                quality={90}
                format="webp"
                loading="eager"
              />
            </div>
          </div>

          <!-- Decorative Element -->
          <div
            class="bg-primary/20 absolute -bottom-4 -left-4 h-32 w-32 animate-pulse rounded-full blur-xl"
          >
          </div>
        </div>
      </div>
    </div>
  </div>
</section>
