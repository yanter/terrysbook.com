---
import Button from "@components/button/Button.astro";
import { getLocaleFromUrl } from "@js/localeUtils";
import { useTranslations } from "@js/translationUtils";
import { Image } from "astro:assets";
import { Icon } from "astro-icon/components";

const currLocale = getLocaleFromUrl(Astro.url);
const t = useTranslations(currLocale);
---

<section
  id="hero-centered"
  class="dark bg-background text-foreground relative flex min-h-[100svh] items-center overflow-hidden pt-16"
>
  <!-- Backdrop Grid Pattern -->
  <div class="text-foreground absolute inset-0" aria-hidden="true">
    <svg class="absolute h-full w-full opacity-7" xmlns="http://www.w3.org/2000/svg">
      <defs>
        <pattern id="hero-grid-pattern" width="40" height="40" patternUnits="userSpaceOnUse">
          <path d="M 40 0 L 0 0 0 40" fill="none" stroke="currentColor" stroke-width="1"></path>
        </pattern>
      </defs>
      <rect width="100%" height="100%" fill="url(#hero-grid-pattern)"></rect>
    </svg>
  </div>

  <!-- Main Content -->
  <div class="site-container relative z-10">
    <div class="mx-auto max-w-4xl text-center">
      <h1
        class="from-primary font-heading mb-6 bg-gradient-to-r to-white bg-clip-text text-5xl text-transparent md:text-7xl"
        data-mos="fade-up"
        data-mos-anchor="#hero-centered"
        data-mos-duration="1.2"
      >
        {t("hero_text")}
      </h1>
      <p
        class="text-base-400 mb-12 text-xl text-pretty"
        data-mos="fade-in"
        data-mos-delay="1"
        data-mos-anchor="#hero-centered"
        data-mos-duration="1.2"
      >
        {t("hero_description")}
      </p>
      <div
        class="flex justify-center"
        data-mos="fade-in"
        data-mos-delay="2"
        data-mos-anchor="#hero-centered"
        data-mos-duration="1.2"
      >
        <Button href="#contact" arrow="right"> Let's Work Together </Button>
      </div>
    </div>
  </div>

  <!-- Accent Light -->
  <div class="bg-primary/20 absolute -top-40 -right-40 size-96 rounded-full blur-3xl xl:size-114">
  </div>
  <div class="bg-primary/20 absolute -bottom-40 -left-40 size-96 rounded-full blur-3xl xl:size-114">
  </div>
</section>
